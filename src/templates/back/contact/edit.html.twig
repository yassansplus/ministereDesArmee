{% extends 'base.html.twig' %}

{% block title %}Editer un contact{% endblock %}

{% block body %}
    <h1 class="font-weight-bold mb-4">Edition du contact</h1>

    {{ include('back/contact/_form.html.twig', {'button_label': 'Mise à jour'}) }}

    <div class="mt-4">
        <a href="{{ path('back_contact_index') }}" class="btn btn-primary">Retour au menu</a>

        {{ include('back/contact/_delete_form.html.twig') }}
    </div>
{% endblock %}
