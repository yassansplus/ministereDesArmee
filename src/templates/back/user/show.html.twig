{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Nom d'utilisateur</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_user_index') }}" class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('back_user_edit', {'id': user.id}) }}" class="btn btn-primary">Editer</a>

    {{ include('back/user/_delete_form.html.twig') }}
{% endblock %}
