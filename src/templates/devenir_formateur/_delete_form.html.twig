<form method="post" action="{{ path('back_formateur_devenir_formateur_delete', {'id': devenir_formateur.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devenir_formateur.id) }}">
    <button class="btn">Delete</button>
</form>
