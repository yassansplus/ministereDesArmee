{% extends 'base.html.twig' %}

{% block title %}DevenirFormateur index{% endblock %}

{% block body %}
    <h1>DevenirFormateur index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Title</th>
            <th>Content</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for devenir_formateur in devenir_formateurs %}
            <tr>
                <td>{{ devenir_formateur.id }}</td>
                <td>{{ devenir_formateur.title }}</td>
                <td>{{ devenir_formateur.content }}</td>
                <td>
                    <a href="{{ path('back_formateur_devenir_formateur_edit', {'id': devenir_formateur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('back_formateur_devenir_formateur_new') }}">Create new</a>
{% endblock %}
