{% extends 'base.html.twig' %}

{% block title %}ChatBotSay{% endblock %}

{% block body %}
    <h1>ChatBotSay</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ chat_bot_say.id }}</td>
        </tr>
        <tr>
            <th>Content</th>
            <td>{{ chat_bot_say.content }}</td>
        </tr>
        <tr>
            <th>IsRoot</th>
            <td>{{ chat_bot_say.isRoot ? 'Yes' : 'No' }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('back_chat_bot_say_index') }}">back to list</a>

    <a href="{{ path('back_chat_bot_say_edit', {'id': chat_bot_say.id}) }}">edit</a>

    {{ include('chat_bot_say/_delete_form.html.twig') }}
{% endblock %}
