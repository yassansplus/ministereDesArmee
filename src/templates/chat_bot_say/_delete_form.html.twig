<form method="post" action="{{ path('back_chat_bot_say_delete', {'id': chat_bot_say.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chat_bot_say.id) }}">
    <button class="btn">Delete</button>
</form>
